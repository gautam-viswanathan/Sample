import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Suicide Prusuation',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
